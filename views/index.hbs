<!DOCTYPE html>
<html>
<head>
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black" />
    <meta http-equiv="refresh" content="30" >
    <title>HomeInfoServer</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css" >
    <style>
        .Item {
            float:left;
            margin: 20px;
            font-size: 30px;
        }
        .Item-Small {
            float:left;
            margin: 20px;
            font-size: 20px;
        }
        .Icon {
            float: left;
            font-size: 40px;
        }
        .Small {
            float: left;
            font-size: 16px;
            color: gray;
        }
    </style>
</head>
<body>
    <h1 style="text-align: right">{{dateAndTime}}</h1>
    <ul class="list-group list-group-flush">
        <li class="list-group-item">
            <div class="Icon">☁️</div>
            <div class="Item"><b>{{weather.temp}}</b></div>
            <div class="Small">{{weather.feelslike}}</div>
            <div class="Item">{{weather.humidity}}</div>
            <div class="Item">{{weather.windspeed}}</div>
            <div class="Item Small">{{weather.tempTomorrow}}</div>
        </li>
        <li class="list-group-item">
            <div class="Icon">🗑️</div>
            <div class="Item"><b>{{waste.type}}</b></div>
            <div class="Item">{{waste.dateSimple}}</div>
            <div class="Item">{{waste.day}}</div>
        </li>
        <li class="list-group-item">
            <div class="Icon">🚆️</div>
            {{#each trains}}
                <div class="Item-Small"><b>{{line}}</b> &nbsp;&nbsp;{{destination}} <div class="Right">{{minutes}}</div></div>
            {{/each}}
        </li>
        <li class="list-group-item">
            <div class="Icon">🛍️</div>
            {{#each markets}}
                <div class="Item">{{location}}<div class="Right">{{date}}</div></div>
            {{/each}}
        </li>
        <li class="list-group-item">
            <div class="Icon">📰️</div>
            <div class="Item-Small"><i>"{{rssFeed.title}}"</i></div>
        </li>
    </ul>
</body>
</html>
